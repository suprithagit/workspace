package preInterview;

import java.util.Arrays;
import java.util.Collections;
import java.util.List;

public class PrintDuplicates {

	public static void main(String[] args) {
		List<Integer> randnum=Arrays.asList(1,3,4,2,1,3,6,3,9,8,9,4,3);
		Collections.sort(randnum);
		System.out.println(randnum);
		
		for(int i=0;i<randnum.size()-1;i++) {
			for(int j=i+1;j<randnum.size();j++) {
				int count=0;
				if(randnum.indexOf(i)==randnum.indexOf(j)) {
					System.out.println(randnum.indexOf(i));
					count++;
				}
			}
		}
		
	}
}
